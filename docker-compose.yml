services:
  app:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    environment:
      - SESSION_SECRET=${SESSION_SECRET:-change-me-in-production}
      - OLLAMA_HOST=http://localhost:11434
    command: ["python", "cli.py", "review", "serve"]
